name: Run Cucumber Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 22
      uses: actions/setup-java@v4
      with:
        java-version: '22'
        distribution: 'temurin'

    - name: Determine Changed Feature Files
      id: changed_files
      run: |
        echo "Checking for changed feature files..."
        git fetch origin main  # Ensure the main branch is available
        CHANGED_FILES=$(git diff --name-only origin/main | grep '.feature' || true)
        echo "Changed files: $CHANGED_FILES"
        if [ -z "$CHANGED_FILES" ]; then
          echo "No changed feature files detected."
          echo "run_tests=false" >> $GITHUB_ENV
        else
          echo "run_tests=true" >> $GITHUB_ENV
          echo "features=$CHANGED_FILES" >> $GITHUB_ENV
        fi
      shell: /usr/bin/bash -e {0}

    - name: Clean the project
      if: env.run_tests == 'true'
      run: mvn clean

    - name: Install dependencies
      if: env.run_tests == 'true'
      run: mvn install -DskipTests

    - name: Run Cucumber Tests
      if: env.run_tests == 'true'
      run: mvn test -Dcucumber.options="${{ env.features }} --plugin com.aventstack.extentreports.cucumber.adapter.ExtentCucumberAdapter:"

    - name: List target directory files
      if: env.run_tests == 'true'
      run: ls -R target

    - name: Upload Extent Reports
      if: success() && env.run_tests == 'true'
      uses: actions/upload-artifact@v4
      with:
        name: extent-reports
        path: target/extent-reports/*
