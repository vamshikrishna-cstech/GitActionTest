name: CI Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      tag:
        description: 'Cucumber Tag to run'
        required: false
        type: choice
        options:
          - '@smoketest'
          - '@regression'

jobs:
  build-check:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Fetch all history for proper diff comparison

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Cache Maven dependencies
      uses: actions/cache@v4
      with:
        path: ~/.m2/repository
        key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
        restore-keys: |
          ${{ runner.os }}-maven-

    - name: Clean the project
      run: mvn clean

    - name: Install dependencies
      run: mvn install -DskipTests

    - name: Build verification
      run: mvn compile

  pr-check:
    runs-on: ubuntu-latest
    needs: build-check  # Run this job only after build-check completes successfully
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Fetch all history to ensure we can detect changes

    - name: Detect Changed Files
      id: detect-changes
      run: |
        git fetch --all
        
        if [ "${{ github.event_name }}" == "pull_request" ]; then
          CHANGED_FILES=$(git diff --name-only origin/${{ github.base_ref }}...${{ github.sha }})
        else
          CHANGED_FILES=$(git diff --name-only HEAD^ HEAD)
        
        echo "Changed files: $CHANGED_FILES"
        
        SAFE_CHANGED_FILES=$(echo "$CHANGED_FILES" | tr '\n' ' ')
        echo "CHANGED_FILES=$SAFE_CHANGED_FILES" >> $GITHUB_ENV

    - name: Set TAG_STRING based on changed files
      id: set_tag_string
      run: |
        TAG_STRING=""
        for file in $CHANGED_FILES; do
          if grep -q '@regression' "$file"; then
            TAG_STRING="@regression"
          elif grep -q '@smoketest' "$file"; then
            TAG_STRING="@smoketest"
          fi
        done

        if [ -z "$TAG_STRING" ]; then
          echo "No relevant tags found, skipping tests."
        fi

        echo "TAG_STRING=$TAG_STRING" >> $GITHUB_ENV

    - name: Run Cucumber Tests
      if: env.TAG_STRING != ''
      run: |
        echo "Running tests with TAG_STRING: $TAG_STRING"
        mvn test -Dcucumber.filter.tags="$TAG_STRING" -Dcucumber.plugin="com.aventstack.extentreports.cucumber.adapter.ExtentCucumberAdapter:"
      continue-on-error: true

    - name: List target directory files
      run: ls -R target

    - name: Upload Extent Reports
      if: success() || failure()
      uses: actions/upload-artifact@v4
      with:
        name: extent-reports
        path: target/extent-reports/*
